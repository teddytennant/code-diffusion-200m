# RTX 5060 Ti (Blackwell, sm_120) install.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch
#   pip install -r requirements-5060ti.txt
#
# Notes:
#   - torch must be the cu128 build (>=2.7) or you get "no kernel image is
#     available for execution on the device" at the first CUDA op. Install it
#     from the pytorch index ABOVE, before this file.
#   - flash-attn is intentionally omitted — no clean sm_120 wheel. The model
#     falls back to SDPA's flash kernel (configs/5060ti.yaml sets
#     flash_attention: false).
#   - bitsandbytes >=0.45 is the first release with Blackwell kernels for the
#     8-bit AdamW optimizer. If even that fails, the loop auto-falls back to
#     torch AdamW (a bit more VRAM, still fits).
transformers>=4.40.0
tokenizers>=0.15.0
datasets>=2.18.0
bitsandbytes>=0.45.0
anthropic>=0.40.0
tiktoken>=0.6.0
human-eval @ git+https://github.com/openai/human-eval.git
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
wandb>=0.17
pytest>=8.0
numpy>=1.26
